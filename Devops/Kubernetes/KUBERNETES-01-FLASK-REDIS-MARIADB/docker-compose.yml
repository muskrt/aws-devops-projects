version: "3"
services:
  web:
    build: . 
    restart: always
    # volumes:
    #   - type: bind 
    #     source: ./
    #     target: /APP

  REDISDB:
    image: redis
    ports:
      - "6379:6379"

  MARIADB:
    image: mariadb
    ports:
      - "3306:3306"
